# Voice-AI-agent-Patient-Registration-System
Voice AI Patient Registration System — A voice agent on a real U.S. phone number (Vapi + GPT-4o) that conversationally collects patient demographics, confirms them by read-back, and persists via the same REST API reviewers can query. FastAPI + SQLAlchemy + SQLite, soft deletes, call transcripts, vanilla dashboard.
